#include <stdio.h>
#include <math.h>

double a_to_b(double a)
{
    double b, i, j;
    j = pow(sin(a), 2) - 3;
    return b;
}

int main()
{
    int n, m;
    double A[50][50], B[50][50];
    return 0;
}
